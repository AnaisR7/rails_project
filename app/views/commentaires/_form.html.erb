<%= form_for(@commentaire) do |f| %>
  <% if @commentaire.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@commentaire.errors.count, "error") %> prohibited this commentaire from being saved:</h2>

      <ul>
      <% @commentaire.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field displayN">
    <%= f.label :post_id %>
    <%= f.text_field :post_id, :class => 'form-control', :value => @post.id %><br>
  </div>
  <div class="field displayN">
    <%= f.label :date %><br>
    <%= f.date_select :date, :class => 'form-control' %>
  </div>
  <div class="row">
    <div class="col-md-3 col-xs-6">
      <div class="field">
        <%= f.label :auteur %><br>
        <%= f.text_field :auteur, :class => 'form-control' %>
      </div>      
    </div>
  </div>
  <div class="row">
    <div class="col-md-8">
      <div class="field">
        <%= f.label :contenu %><br>
        <%= f.text_area :contenu, :class => 'form-control' %>
      </div>        
    </div>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
